@model hirelonghorn.Models.Interview

@{
    ViewData["Title"] = "SelectDateEdit";
}

<form asp-action="SelectRoomEdit">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="InterviewId" name="id" />

    <div class="form-group">
        <label>Select a Starting Date:</label>
        <input type="date" name="SelectedStartingDate" class="form-control" />
    </div>
    <div class="form-group">
        <input type="submit" value="Next Step" class="btn btn-success" />
    </div>
</form>
<div>
    <a asp-action="Index" asp-controller="Home">Back to List</a>
</div>
